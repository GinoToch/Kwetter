apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: users-data
  name: users-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
