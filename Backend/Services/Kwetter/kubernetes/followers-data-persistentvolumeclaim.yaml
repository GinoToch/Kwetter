apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: followers-data
  name: followers-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
